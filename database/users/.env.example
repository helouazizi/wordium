POSTGRES_USER=users_user
POSTGRES_PASSWORD=users_pass
POSTGRES_DB=users_db
# POSTGRES_HOST=    # if you need to connect to db from local host use this if it service is in docker use the service name to connect 